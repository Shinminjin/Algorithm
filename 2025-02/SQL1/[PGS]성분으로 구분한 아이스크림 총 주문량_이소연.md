```sql
SELECT
    INGREDIENT_TYPE,
    SUM(TOTAL_ORDER) AS TOTAL_ORDER
FROM FIRST_HALF fh
LEFT JOIN ICECREAM_INFO ice 
    ON fh.FLAVOR = ice.FLAVOR
GROUP BY INGREDIENT_TYPE
ORDER BY fh.TOTAL_ORDER ASC;
```
